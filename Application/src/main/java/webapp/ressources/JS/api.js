


$("input[name='formConnexion']").on("submit", function(e){
    e.preventDefault();
    alert("salut");
    /*var form = this;
    checkIndex('upload/segments.gen').done(function() {
        form.submit(); // submit bypassing the jQuery bound event
    }).fail(function () {
        alert("No index present!");
    });*/
});